spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/bankbootdb
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
      auto_quote_keyword: true
    show-sql: true
